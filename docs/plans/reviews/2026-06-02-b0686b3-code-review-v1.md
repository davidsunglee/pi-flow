Review provenance: code-reviewer openai-codex/gpt-5.5:xhigh b0686b3264608696b937f610799011350f4609ce..687e8e001fb79f45fd436de11cc277fe80141966

### Outcome

**Verdict:** Approved

**Reasoning:** The reviewed range satisfies the runtime/config/UI integration requirements with clean settings-store ownership, slimmed working-state coordination, and store-driven editor/header rendering. I also verified `pnpm --dir packages/pi-flow-ux test` passes with 137 tests and found no Critical or Important production-readiness issues.

### Strengths

- `TuiSettingsStore` centralizes canonical defaults, validation, subscriptions, session loading, and `/tui` command handling; invalid `header.logo` values are rejected before mutation while valid values are persisted and emitted (`packages/pi-flow-ux/extensions/settings.ts:21-28`, `packages/pi-flow-ux/extensions/settings.ts:133-224`).
- `WorkingSnapshot` is reduced to `{ visible, state }`, and `WorkingCoordinator` now focuses on working-state lifecycle plus host working-row suppression without settings ownership (`packages/pi-flow-ux/extensions/working.ts:5-8`, `packages/pi-flow-ux/extensions/working.ts:56-153`).
- Header variants are declared in canonical order, default to `bracket`, read `store.get().header.logo` during render, and unsubscribe idempotently on component or handle disposal (`packages/pi-flow-ux/extensions/header.ts:54-94`).
- The editor now reads `working.indicator` from the settings store, subscribes to both working-state and settings changes, updates timer cadence from the active indicator, and tears down timers/subscriptions on dispose (`packages/pi-flow-ux/extensions/editor.ts:648-662`, `packages/pi-flow-ux/extensions/editor.ts:722-737`).
- `index.ts` wires the store before the working coordinator and installs footer/editor/header handles with restart/shutdown teardown (`packages/pi-flow-ux/extensions/index.ts:14-36`).

### Issues

#### Critical (Must Fix)

_None._

#### Important (Should Fix)

_None._

#### Minor (Nice to Have)

- **packages/pi-flow-ux/extensions/header.test.ts:73: Header variant tests use implementation constants as the oracle**
  - **What:** The variant rendering test derives expected row counts from `LOGO_VARIANTS`, and the unknown-variant fallback test only checks row count.
  - **Why it matters:** These tests would not catch accidental drift in the exact frame characters, or a fallback to another one-row variant instead of `bracket`, even though the production code currently matches the spec.
  - **Recommendation:** Strip ANSI from rendered logo rows and assert exact plain-text arrays for `bracket`, `sidebar`, `rounded`, `squared`, plus exact `bracket` fallback for an unknown value.

### Recommendations

_None._
