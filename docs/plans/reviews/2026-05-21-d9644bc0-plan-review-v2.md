**Reviewer:** openai-codex/gpt-5.5 via pi

### Outcome

**Verdict:** Approved with concerns

**Reasoning:** The plan is structurally buildable and covers the rebrand end-to-end, including the spec's chosen `idea`-tool approach. Waiving the Important Task 18 concern because the plan explicitly documents that no executable LLM-orchestrator harness exists and substitutes focused contract tests, but the user should confirm that surrogate coverage is acceptable for the spec's literal end-to-end test requirement.

### Strengths

- Tasks 1–5 comprehensively cover the TypeScript extension surface, including storage path/prefix changes, router canonicalization, workflow descriptions, and smoke-test fixtures.
- Tasks 6, 8, 12, 13, and 16 carry the provenance-chain rename from `source_todo` / `{SOURCE_TODO}` / `--source-todo` to `source_idea` / `{SOURCE_IDEA}` / `--source-idea` through parsers, prompts, helpers, and planner output.
- Tasks 11, 12, 14, and 15 honor the spec's chosen approach by routing skill-level idea access through the built-in `idea` tool for scout, generate-plan, execute-plan, and fastlane.
- Task 19 provides a strong final regression sweep with repo-wide greps for legacy prefixes, paths, external `todo` tool references, and source placeholder leaks, followed by `pnpm run check`.

### Issues

#### Critical (Must Fix)

_None._

#### Important (Should Fix)

- **Task 18: Surrogate test does not literally exercise generate-plan → execute-plan end-to-end**
  - **What:** The spec requires a new test demonstrating that `generate-plan` and `execute-plan` complete end-to-end with only the built-in `idea` tool registered. Task 18 explicitly narrows that to surrogate checks: loaded extension surface inspection, grep-like assertions over `generate-plan/SKILL.md` and `execute-plan/SKILL.md`, and a simulated execute-plan Step 16.2 read/update round trip through the `idea` tool. It does not run generate-plan against an `IDEA-<id>` input, produce a real plan, then run execute-plan against that plan.
  - **Why it matters:** A break in the actual orchestration handoff, prompt rendering, generated plan `**Source:**` header, or execute-plan parsing path could still pass the surrogate test while failing the spec's literal acceptance criterion.
  - **Recommendation:** If a real orchestration harness exists or can be added at reasonable cost, replace or supplement Task 18 with a true generate-plan-to-execute-plan integration test. If not, keep the surrogate test but explicitly confirm with the user/spec owner that this documented limitation is acceptable.

#### Minor (Nice to Have)

_None._

### Recommendations

- Confirm Task 18's surrogate coverage with the user before execution if the literal end-to-end test requirement is non-negotiable.
