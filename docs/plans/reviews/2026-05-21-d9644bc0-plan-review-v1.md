**Reviewer:** openai-codex/gpt-5.5 via pi

### Outcome

**Verdict:** Not approved

**Reasoning:** The plan is broadly comprehensive, but Task 18 creates literal `todo tool`/`` `todo` tool`` strings in a new test file while Task 19 requires repo-wide greps for those strings to return zero matches, making the plan unbuildable as written.

### Strengths

- Tasks 1–8 cover the TypeScript storage/router/user-facing surface and Python provenance/CLI chain in a coordinated order.
- Tasks 10–16 cover the skill prose and planner header changes needed to preserve the spec/plan/execute provenance chain.
- The plan honors the spec's chosen approach by routing generate-plan, execute-plan, fastlane, and scout artifact access through the built-in `idea` tool.
- Task 19 provides a useful final regression sweep and full-suite verification command.

### Issues

#### Critical (Must Fix)

- **Task 18 / Task 19: New test violates final repo-wide grep requirements**
  - **What:** Task 18's proposed `idea-tool-only.test.mjs` includes assertion messages and test names containing phrases such as `external \`todo\` tool`, `no external \`todo\` tool`, and `no \`todo\` tool required`. Task 19 Step 3 then requires `grep -rnE '\btodo tool\b' packages/pi-flow-core/`, `grep -rnE '`todo` tool' packages/pi-flow-core/`, and related greps to return zero matches across all of `packages/pi-flow-core/`.
  - **Why it matters:** An implementer following Task 18 exactly will add matches that Task 19 explicitly treats as failures, so the final acceptance criteria cannot be satisfied without deviating from the plan.
  - **Recommendation:** Reword Task 18's test names/messages/comments to avoid the exact forbidden `todo tool` phrases, or narrow Task 19's grep scope/exemptions if tests are intentionally allowed to mention the legacy tool name.

#### Important (Should Fix)

- **Task 9: Test-count instructions are internally inconsistent**
  - **What:** Task 9 Step 1 says to include “nine test methods analogous to the existing test class” but lists ten analogous methods, then adds an additional `test_legacy_todo_prefix_does_not_match`. The acceptance criteria then say `test_idea_input_shape.py` should have 10 passing tests.
  - **Why it matters:** A worker may implement 11 tests from the step list and fail the acceptance criterion that expects 10, or implement 10 tests and omit one named case from the step list.
  - **Recommendation:** Make the step list and acceptance criterion agree on the exact test count and exact method names.

#### Minor (Nice to Have)

_None._

### Recommendations

- Before execution, reconcile the grep-based acceptance checks with any intentionally retained legacy-name assertions in tests so the final sweep is mechanically satisfiable.
