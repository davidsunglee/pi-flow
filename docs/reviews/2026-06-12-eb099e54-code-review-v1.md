**Reviewer:** openai-codex/gpt-5.5 via codex

### Outcome

**Verdict:** Approved

**Reasoning:** The implementation satisfies the project-aware flow-config resolution requirements across the shared Python resolver, dispatch/provenance consumers, doctor diagnostics, setup guidance, docs, and workflow call sites. I found no Critical or Important issues; the only finding is a non-blocking documentation polish item, and the full `pnpm check` suite passes.

### Strengths

- `skills/_shared/scripts/flow_config_resolution.py:37` centralizes the Python resolution contract with explicit, project-local, and user/global precedence, while preserving the ordered `searched` list used by downstream error text.
- `skills/_shared/scripts/resolve-model-dispatch.py:88` and `skills/_shared/scripts/validate-review-provenance.py:108` correctly share the resolver and reuse the selected-path `searched` list when JSON parsing fails, preventing unintended fallback after selection.
- `extensions/doctor.ts:196` adds an in-process diagnostic mirror, and `extensions/doctor.ts:939` reports flow-config scope without feeding advisory state into package-skew verdicts.
- `docs/flow-config-setup.md:5` and `docs/flow-config-setup.md:141` document the precedence and provide a direct `resolve-flow-config` verification path for users.
- Test coverage is broad: resolver scope/error tests, migrated helper precedence tests, provenance forwarding tests, doctor/setup tests, guardrail strings, and package-level `pnpm check` all passed.

### Issues

#### Critical (Must Fix)

_None._

#### Important (Should Fix)

_None._

#### Minor (Nice to Have)

- **docs/flow-config-setup.md:18: Project-local copy command omits parent-directory creation**
  - **What:** The new project-local setup example copies directly to `.pi/flow.json`, but a fresh project may not have a `.pi` directory yet.
  - **Why it matters:** First-time users following the new project-local path can hit a avoidable `cp: .pi/flow.json: No such file or directory` error before they can verify resolution.
  - **Recommendation:** Add `mkdir -p .pi` before the project-local copy command, and consider `mkdir -p ~/.pi/agent` before the user/global example for symmetry.

### Recommendations

_None._
