**Reviewer:** openai-codex/gpt-5.5 via codex

### Outcome

**Verdict:** Approved

**Reasoning:** The coordinator-dispatch change cleanly separates coordinator runtime capability from leaf-worker provider-prefix dispatch, preserves the leaf-worker resolution contract, and passes the relevant helper, guardrail, package, and aggregate test suites. I found no Critical or Important production blockers.

### Strengths

- `packages/pi-flow-core/skills/_shared/scripts/resolve-coordinator-dispatch.py:61` implements a dedicated coordinator validator with the required hardcoded `cli: "pi"` output, missing-section handling, and wholesale `modelChain` validation.
- `packages/pi-flow-core/skills/_shared/coordinator-dispatch.md:9` rewrites the shared coordinator procedure around the helper, exact `modelChain` iteration, and canonical hard-stop templates without retaining the old tier-chain fallback semantics.
- `packages/pi-flow-core/skills/_shared/scripts/tests/test_resolve_coordinator_dispatch.py:37` covers valid chains, opaque entries, missing/unusable config templates, and leaf-dispatch coexistence against the decoupled fixture.
- `packages/pi-flow-core/__tests__/guardrail-strings.test.mjs:150` locks the new coordinator templates and the absence of stale four-tier coordinator-chain strings in the affected shared/refiner docs.

### Issues

#### Critical (Must Fix)

_None._

#### Important (Should Fix)

_None._

#### Minor (Nice to Have)

- **packages/pi-flow-core/docs/model-tier-setup.md:50: Dispatch CLI example still names `pi`**
  - **What:** The schema example immediately above routes `openai-codex` to `codex`, but the explanatory bullet still says dispatch values are CLI names such as `claude`, `pi`.
  - **Why it matters:** This does not break runtime behavior, and `pi` remains a possible CLI value for leaf dispatch, but it slightly blurs the new docs' main point that no provider-prefix dispatch entry needs to resolve to `pi`.
  - **Recommendation:** Change the example list to `claude`, `codex` or add a short note that `pi` is only an optional leaf-dispatch CLI, not required for coordinator dispatch.

### Recommendations

- Keep the aggregate `npm_config_cache` isolation changes in their own release note or changeset entry, since they are outside the coordinator capability/dispatch requirement and will be easier to audit independently.
