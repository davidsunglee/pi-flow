**Reviewer:** openai-codex/gpt-5.5 via codex

### Outcome

**Verdict:** Approved

**Reasoning:** The current diff satisfies the requested scope-aware doctor, shared bare-node resolver, stable dispatcher, setup migration, and readable report behavior. I found no Critical or Important production-readiness issues, and the required `pnpm --filter @aphotic/pi-flow-core check` command passed.

### Strengths

- `packages/pi-flow-core/extensions/lib/effective-package.mjs:13` centralizes the resolution primitives, npm-spec parsing, aggregate/core identity normalization, trust checks, effective-root selection, user-root resolution, and path abbreviation in a bare-node module shared by doctor and the dispatcher.
- `packages/pi-flow-core/bin/pi-flow-dispatch.mjs:71` keeps the launcher resilient: it probes known bootstrap locations, imports the shared resolver when available, falls back to the bootstrap user core on resolver failure, and returns an actionable null target when no user install exists.
- `packages/pi-flow-core/extensions/doctor.ts:685` models the helper dispatcher on separate stale-bytes and runtime-target axes, which preserves the distinction between an out-of-date installed launcher and the core it will resolve for the current cwd.
- `packages/pi-flow-core/extensions/doctor.ts:1591` derives default `--fix` repairs from the effective diagnosis, so trusted project overrides repair project-scope agents while user-effective cases repair user-scope agents.
- `packages/pi-flow-core/extensions/setup.ts:286` preserves the never-overwrite posture while migrating owned legacy pi-flow-core symlinks to a real dispatcher copy and leaving divergent foreign entries untouched.
- Final remediation coverage is good: `packages/pi-flow-core/extensions/doctor.ts:1158` and `packages/pi-flow-core/extensions/setup.ts:332` now restore executable bits even on identical dispatcher files, with focused tests for skip and refresh paths.

### Issues

#### Critical (Must Fix)

_None._

#### Important (Should Fix)

_None._

#### Minor (Nice to Have)

_None._

### Recommendations

- Keep the full `pnpm --filter @aphotic/pi-flow-core check` gate for future doctor/setup/dispatcher changes; this behavior spans bare-node launchers, TypeScript extension code, packaging, and helper tests.

Verification run: `pnpm --filter @aphotic/pi-flow-core check` exited 0.
