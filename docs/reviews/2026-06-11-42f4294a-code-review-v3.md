**Reviewer:** openai-codex/gpt-5.5 via codex

### Outcome

**Verdict:** Approved

**Reasoning:** The implementation matches the requested architecture: shared bare-node resolution logic, dispatcher installation, scope-aware doctor classification, grouped reporting, and scoped repair behavior are covered by focused tests, and the full package check passes.

### Strengths

- `packages/pi-flow-core/extensions/lib/effective-package.mjs:13` centralizes the resolution primitives, package identity normalization, trust handling, effective-root selection, and path abbreviation in a bare-node module, reducing resolver drift between doctor and the dispatcher.
- `packages/pi-flow-core/bin/pi-flow-dispatch.mjs:71` keeps the installed helper stable and resilient: bootstrap/import failures and malformed configuration degrade to the user core instead of crashing.
- `packages/pi-flow-core/extensions/doctor.ts:685` models the dispatcher as two independent axes, stale installed bytes and runtime target resolution, which avoids masking target divergence behind byte equality.
- `packages/pi-flow-core/extensions/doctor.ts:1616` threads `--strict` and `--all` through the command handler with the correct notify severity behavior.
- The test coverage is broad and scenario-driven, including effective override cases, aggregate/direct interop, dispatcher refresh behavior, scoped `--fix`, setup migration, and full package verification.

### Issues

#### Critical (Must Fix)

_None._

#### Important (Should Fix)

_None._

#### Minor (Nice to Have)

- **packages/pi-flow-core/extensions/doctor.ts:1557: `--fix` guidance ignores npm declarations**
  - **What:** `declaredSpecsNamingTarget` only checks local package specs via `readDeclaredLocalSpecs`, so a target already named by `packages: ["npm:@aphotic/pi-flow-core"]` or `["npm:@aphotic/pi-flow"]` is treated as unnamed for fix-report guidance.
  - **Why it matters:** The repair itself remains correct, but users can receive a misleading note telling them to add or update `.pi/settings.json` even though an npm package entry already names the effective target.
  - **Recommendation:** Include npm rows in the declared-spec scan by resolving them with the same shared `resolveSpecToCoreRoot` path used by `declaredSurfaceReports`, then suppress the durability note when the resolved root matches the target.

### Recommendations

- Keep the existing full `pnpm --filter @aphotic/pi-flow-core check` gate on changes to doctor/setup/dispatcher; this diff’s behavior is spread across TypeScript command code, bare-node launchers, and package/discovery tests, so the broad suite is pulling real weight.

Verification run: `pnpm --filter @aphotic/pi-flow-core check` exited 0.
