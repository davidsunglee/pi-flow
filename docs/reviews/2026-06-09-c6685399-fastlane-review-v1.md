**Reviewer:** openai-codex/gpt-5.5 via codex

### Outcome

**Verdict:** Approved

**Reasoning:** The diff stays scoped to the packaged Flow refine prompt plus `pi-flow-core` prompt-text regression tests, and it preserves the generic `pi-ideas` defaults. The new prompt covers the required clarification behavior and the relevant `pi-flow-core` and `pi-ideas` test suites pass.

### Strengths

- `packages/pi-flow-core/ideas.json:3` explicitly tells the model to read the existing idea, inspect the current `## Open Questions`, treat answerable items as clarification targets, and skip that step gracefully when the section is absent.
- `packages/pi-flow-core/ideas.json:3` separates conversation-time clarifying questions from the final artifact section, keeps the no-premature-rewrite and recommendation guidance, preserves the canonical section order, and sets `None.` as the resolved default.
- `packages/pi-flow-core/extensions/ideas-refine-prompt.test.ts:16` resolves the packaged prompt through `resolveIdeasConfig` with isolated home/cwd directories, matching runtime config resolution while avoiding local override leakage.
- `packages/pi-flow-core/extensions/ideas-refine-prompt.test.ts:30` through `packages/pi-flow-core/extensions/ideas-refine-prompt.test.ts:123` assert the key prompt-text requirements directly: placeholder retention, canonical order, inspect-existing guidance, conversation/final-section distinction, `None.` default, deferred-only residue, and update-action wording.

### Issues

#### Critical (Must Fix)

_None._

#### Important (Should Fix)

_None._

#### Minor (Nice to Have)

_None._

### Recommendations

_None._

Verification performed: `pnpm --filter @aphotic/pi-flow-core run test:node`, `pnpm --filter @aphotic/pi-flow-core run typecheck`, `pnpm --filter @aphotic/pi-ideas run test:node`, and `git diff --check b788bcfa810417237b3880bcde860e58731a13e3..a2a9344dabc31c2bede273016529bab1c999f7f6` all passed. A search for `pi-flow-core` references under `packages/pi-ideas` returned no matches.
