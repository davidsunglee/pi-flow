**Reviewer:** openai-codex/gpt-5.5 via pi

### Outcome

**Verdict:** Approved

**Reasoning:** The refactor matches the single-entrypoint `/tui` architecture, removes the legacy status/working entrypoints and configs, and the full workspace `pnpm test` run passed. Only a minor stale release-documentation reference remains; it does not affect runtime behavior or packaging correctness.

### Strengths

- Clear unified configuration boundary in `packages/pi-flow-ux/extensions/working.ts:27-39` and `packages/pi-flow-ux/extensions/working.ts:48-59`: `tui.json` normalization is canonical, placeholders are normalized, and the packaged default path correctly targets the package-root `tui.json`.
- Host working-row suppression is presence-checked and restored safely (`packages/pi-flow-ux/extensions/working.ts:145-156`, `packages/pi-flow-ux/extensions/working.ts:210-219`, `packages/pi-flow-ux/extensions/working.ts:287-295`), matching the requirement to prefer `setWorkingVisible` with a `setWorkingIndicator({ frames: [] })` fallback.
- The visual-effects split is clean and theme-independent: per-state effects are hardcoded in `packages/pi-flow-ux/extensions/effects.ts:77-85`, with state-driven indicator APIs and separate model/thinking text effects in `packages/pi-flow-ux/extensions/effects.ts:103-130`.
- The border editor preserves the responsive layout while applying the new snapshot-driven activity and text effects (`packages/pi-flow-ux/extensions/editor.ts:402-415`, `packages/pi-flow-ux/extensions/editor.ts:610-617`, `packages/pi-flow-ux/extensions/editor.ts:705-722`).
- The new entrypoint wires exactly the intended pieces and command registration path (`packages/pi-flow-ux/extensions/index.ts:10-28`), keeping internal modules out of the manifest.
- The custom header is compact, UI-guarded, and free of legacy powerline/config toggles (`packages/pi-flow-ux/extensions/header.ts:6-16`, `packages/pi-flow-ux/extensions/header.ts:62-78`).
- Verification was strong: `pnpm test` from the repository root completed successfully with 0 failures across the workspace.

### Issues

#### Critical (Must Fix)

_None._

#### Important (Should Fix)

_None._

#### Minor (Nice to Have)

- **packages/pi-flow/README.md:71: Stale release-guard resource names mention removed UX config**
  - **What:** The release section still says the guard requires “the UX footer/working/theme/`working.json`”, even though the implementation now bundles `extensions/index.ts`, `themes/nord.json`, and `tui.json`.
  - **Why it matters:** Maintainer-facing release docs are slightly misleading after the intentional breaking change, even though the actual `pack-aggregate.mjs` guard is correct.
  - **Recommendation:** Update the sentence to name the unified UX entrypoint, Nord theme, and `tui.json`.

### Recommendations

- Consider adding the aggregate README release-resource sentence to the same stale-reference grep used for the UX README so removed config names (`working.json`/`status.json`) cannot linger in maintainer docs.
